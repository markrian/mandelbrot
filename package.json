{
  "name": "mandelbrot",
  "version": "1.0.0",
  "private": true,
  "description": "A simple Mandelbrot set viewer",
  "repository": {
    "type": "git",
    "url": "https://github.com/markrian/mandelbrot.git"
  },
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp node_modules/leaflet/dist/leaflet.*s dist && cp *.js *.html dist"
  },
  "keywords": [],
  "author": "Mark Florian <markrian@gmail.com>",
  "license": "ISC",
  "dependencies": {
    "leaflet": "1.x"
  }
}
